{
  "name": "aeroguard-workspace",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install-all": "npm install && cd server && npm install && cd ../client && npm install",
    "dev:python": "uvicorn ml.api:app --host 127.0.0.1 --port 8000",
    "dev:node": "cd server && npm run dev",
    "dev:react": "cd client && npm run dev",
    "dev": "concurrently --kill-others \"npm run dev:python\" \"npm run dev:node\" \"npm run dev:react\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
